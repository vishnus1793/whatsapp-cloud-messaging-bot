# whatsapp-cloud-messaging-bot

This project sends a daily motivational message to your WhatsApp using Twilio + GitHub Actions.

## 🚀 Setup

1. Clone repo
2. Create virtualenv and install deps:
   ```bash
   pip install -r requirements.txt
